
import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    avatarContainer: {
        overflow: 'hidden',
    },
    avatarPlaceholder: {
        width: '100%',
        height: '100%',
        resizeMode: 'cover',
    },
    avatar: {
        width: '100%',
        height: '100%',
        resizeMode: 'cover',
        position: 'absolute'
    },
})

export default styles
