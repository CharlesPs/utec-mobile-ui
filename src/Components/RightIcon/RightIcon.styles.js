
import {
    StyleSheet
} from 'react-native'

const styles = StyleSheet.create({
    iconContainer:{
        // flex: 1,
        // height: '100%',
        justifyContent: 'center',
        alignItems: 'center',
        alignContent: 'center',
        zIndex: 999

    },
    icon:{
        height: '100%',
        width: 12    ,
        // height: 25,
        resizeMode: 'cover',
        // backgroundColor: 'red',
    },
});

export default styles