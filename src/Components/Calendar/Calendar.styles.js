
import {
    StyleSheet
} from 'react-native'

const styles = StyleSheet.create({
    calendarContainer: {
        flex: 1,
        position: 'relative',
        backgroundColor: '#FFF',
    },
})

export default styles
